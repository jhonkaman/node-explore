// Create Arrays

// Run: node 05-arrays/01-create-arrays.js

// Learn how to create and initialize arrays to store multiple values

let fruits = ["apple", "banana", "orange"];
console.log(fruits);

let numbers = [1, 2, 3, 4, 5];
console.log(numbers);

let mixed = [1, "hello", true, 3.14];
console.log(mixed);

// Array length
console.log(fruits.length);
console.log(numbers.length);
